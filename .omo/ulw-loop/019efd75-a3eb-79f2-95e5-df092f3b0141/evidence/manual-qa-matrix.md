# Manual QA Matrix: Loading and SALT PNG Icons

## Criteria
- C001: Cover-letter submit shows visible generation loading state while `/api/analyze/` is pending.
- C002: Analysis result summary renders S/A/L/T using PNG assets from `docs/images` and the images load in the browser.
- C003: Adjacent analyze flow remains working.

## Evidence Mapping
- Loading state: `loading-desktop.png`, `loading-mobile.png`; generated by a Playwright browser run that held `/api/analyze/` pending and asserted `.loading-spinner` plus `로드맵을 생성하고 있습니다`.
- SALT PNG icons: `salt-icons-desktop.png`, `salt-icons-mobile.png`; generated by a Playwright browser run that asserted four `.stat-icon img` nodes and `naturalWidth > 0`.
- E2E regression: `frontend-e2e-loading-salt-analyze.txt` shows `5 passed` for `loading-salt-icons.spec.js` plus existing `analyze-flow.spec.js`.
- Build: `frontend-build.txt` shows Vite build success and bundled S/A/L/T PNG assets.
- Design check: `frontend-design-test.txt` shows `frontend design verification passed`.

## Cleanup
- Temporary screenshot spec `frontend/tests/e2e/visual-evidence.temp.spec.js` was removed after the screenshot run.
- Playwright browser contexts and webServer processes exited through Playwright.
