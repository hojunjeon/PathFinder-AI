diff --git a/frontend/src/components/result/CompetencyGap.vue b/frontend/src/components/result/CompetencyGap.vue
index edc6e7b..f3f89d1 100644
--- a/frontend/src/components/result/CompetencyGap.vue
+++ b/frontend/src/components/result/CompetencyGap.vue
@@ -2,8 +2,8 @@
   <section class="section-card" id="gap" data-od-id="competency-analysis">
     <div class="section-head">
       <div>
-        <h2>역량 분석</h2>
-        <p class="section-description">직무 핵심 역량별로 지금 해야 할 행동을 한눈에 확인하세요.</p>
+        <h2>역량 지도</h2>
+        <p class="section-description">직무 핵심 역량별 현재 상태와 요구 수준을 비교하고, 지금 해야 할 행동을 확인하세요.</p>
       </div>
       <span class="section-note">점수가 아닌 경험 근거 기반 상태</span>
     </div>
@@ -30,6 +30,30 @@
         </div>
       </div>
 
+      <div class="competency-map" aria-label="역량 지도">
+        <div class="map-legend" aria-hidden="true">
+          <span><i class="legend-current"></i>현재 역량</span>
+          <span><i class="legend-required"></i>직무 요구</span>
+        </div>
+        <div class="map-axis-list">
+          <article v-for="item in items" :key="`axis-${item.key}`" class="axis-card">
+            <div class="axis-copy">
+              <strong>{{ item.keyword }}</strong>
+              <span>{{ item.signal || item.action || statusLabel(item.status) }}</span>
+            </div>
+            <div class="axis-bars" aria-hidden="true">
+              <span class="axis-track current">
+                <i :style="{ width: item.currentWidth }"></i>
+              </span>
+              <span class="axis-track required">
+                <i :style="{ width: item.requiredWidth }"></i>
+              </span>
+            </div>
+            <span :class="['axis-status', `axis-${item.status}`]">상태: {{ statusLabel(item.status) }}</span>
+          </article>
+        </div>
+      </div>
+
       <div class="status-grid">
         <section
           v-for="group in visibleGroups"
@@ -178,6 +202,13 @@ function deriveLegacyItems(gap) {
 
 function createItem(keyword, status, importance, signal, action, index) {
   const clean = cleanKeyword(keyword)
+  const currentWidth = {
+    strength: '88%',
+    articulate: '64%',
+    study: '34%',
+    insufficient_data: '18%',
+  }[status]
+  const requiredWidth = importance === 'preferred' ? '70%' : '92%'
   return {
     key: `${status}-${index}-${clean}`,
     keyword: clean,
@@ -185,9 +216,20 @@ function createItem(keyword, status, importance, signal, action, index) {
     importance,
     signal: String(signal || '').trim(),
     action: String(action || '').trim(),
+    currentWidth,
+    requiredWidth,
   }
 }
 
+function statusLabel(status) {
+  return {
+    strength: '어필 가능',
+    articulate: '답변 정리',
+    study: '학습 필요',
+    insufficient_data: '판단 보류',
+  }[status] || '판단 보류'
+}
+
 function normalizeStatus(value) {
   return ['strength', 'articulate', 'study', 'insufficient_data'].includes(value)
     ? value
@@ -258,6 +300,109 @@ h2 {
   background: var(--surface-warm);
   margin-bottom: var(--space-4);
 }
+.competency-map {
+  border: 1px solid var(--border-soft);
+  border-radius: var(--radius-lg);
+  background: color-mix(in oklab, var(--surface), var(--bg) 55%);
+  padding: var(--space-5);
+  margin-bottom: var(--space-4);
+}
+.map-legend {
+  display: flex;
+  flex-wrap: wrap;
+  justify-content: flex-end;
+  gap: var(--space-4);
+  color: var(--muted);
+  font-size: var(--text-xs);
+  font-weight: 600;
+  margin-bottom: var(--space-4);
+}
+.map-legend span {
+  display: inline-flex;
+  align-items: center;
+  gap: var(--space-2);
+}
+.map-legend i {
+  width: 24px;
+  height: 4px;
+  border-radius: var(--radius-pill);
+}
+.legend-current {
+  background: var(--accent);
+}
+.legend-required {
+  background: var(--danger);
+}
+.map-axis-list {
+  display: grid;
+  gap: var(--space-3);
+}
+.axis-card {
+  display: grid;
+  grid-template-columns: minmax(160px, 1fr) minmax(180px, 280px) auto;
+  align-items: center;
+  gap: var(--space-4);
+  padding: var(--space-3) var(--space-4);
+  border-radius: var(--radius-md);
+  background: var(--bg);
+  box-shadow: var(--elev-ring);
+}
+.axis-copy {
+  display: grid;
+  gap: 3px;
+  min-width: 0;
+}
+.axis-copy strong {
+  color: var(--fg);
+  font-size: var(--text-sm);
+  line-height: 1.35;
+  word-break: keep-all;
+}
+.axis-copy span {
+  color: var(--muted);
+  font-size: var(--text-xs);
+  line-height: 1.45;
+}
+.axis-bars {
+  display: grid;
+  gap: 6px;
+}
+.axis-track {
+  height: 7px;
+  border-radius: var(--radius-pill);
+  background: var(--border-soft);
+  overflow: hidden;
+}
+.axis-track i {
+  display: block;
+  height: 100%;
+  border-radius: inherit;
+}
+.axis-track.current i {
+  background: var(--accent);
+}
+.axis-track.required i {
+  background: color-mix(in oklab, var(--danger), var(--bg) 16%);
+}
+.axis-status {
+  justify-self: end;
+  border-radius: var(--radius-pill);
+  padding: 4px 9px;
+  font-size: var(--text-xs);
+  font-weight: 600;
+  white-space: nowrap;
+  color: var(--fg);
+  background: var(--surface-warm);
+}
+.axis-strength {
+  color: var(--success);
+}
+.axis-articulate {
+  color: var(--warn);
+}
+.axis-study {
+  color: var(--danger);
+}
 .overview-copy {
   display: grid;
   gap: var(--space-1);
@@ -386,6 +531,13 @@ h2 {
 }
 
 @media (max-width: 760px) {
+  .axis-card {
+    grid-template-columns: 1fr;
+    align-items: stretch;
+  }
+  .axis-status {
+    justify-self: start;
+  }
   .status-grid { grid-template-columns: 1fr; }
 }
 @media (max-width: 640px) {
diff --git a/frontend/src/views/AnalyzeResultView.vue b/frontend/src/views/AnalyzeResultView.vue
index 94b99d2..a2ca134 100644
--- a/frontend/src/views/AnalyzeResultView.vue
+++ b/frontend/src/views/AnalyzeResultView.vue
@@ -102,6 +102,77 @@
           <!-- Roadmap Checklist Timeline -->
           <PreparationKeywordBoard :roadmap-items="roadmapItems" />
 
+          <section class="section-card action-plan-card" id="action-plan" data-od-id="result-action-plan">
+            <div class="section-head">
+              <div>
+                <h2>상태별 액션 플래너</h2>
+                <p class="section-description">어필 가능, 답변 정리, 학습 필요 항목을 상태별로 나누고 예상 질문의 답변 전략을 바로 펼쳐봅니다.</p>
+              </div>
+              <span class="section-note">질문 완료 상태 자동 저장</span>
+            </div>
+            <div v-if="actionPlannerGroups.some(group => group.items.length)" class="action-grid">
+              <section
+                v-for="group in actionPlannerGroups"
+                :key="group.type"
+                :class="['action-column', `action-${group.type}`]"
+              >
+                <header class="action-head">
+                  <div>
+                    <strong>{{ group.label }}</strong>
+                    <p>{{ group.description }}</p>
+                  </div>
+                  <span>{{ group.doneCount }}/{{ group.items.length }} 완료</span>
+                </header>
+                <div class="action-items">
+                  <article
+                    v-for="item in group.items"
+                    :key="item.key"
+                    :class="['action-item', { done: item.done }]"
+                  >
+                    <div class="action-check-row">
+                      <input
+                        type="checkbox"
+                        :checked="item.done"
+                        :aria-label="`액션 플래너 ${item.key} 완료`"
+                        @change="toggleTask(item.togglePayload)"
+                      />
+                      <span>
+                        <strong>{{ item.title }}</strong>
+                        <em>{{ item.category }}</em>
+                      </span>
+                    </div>
+                    <p>{{ item.action }}</p>
+                    <button
+                      class="strategy-toggle"
+                      type="button"
+                      :aria-expanded="isStrategyOpen(item.key)"
+                      @click="toggleStrategy(item.key)"
+                    >
+                      {{ isStrategyOpen(item.key) ? '답변 전략 닫기' : '답변 전략 보기' }}
+                    </button>
+                    <div v-if="isStrategyOpen(item.key)" class="strategy-panel">
+                      <div>
+                        <span>예상 질문</span>
+                        <p>{{ item.question.question }}</p>
+                      </div>
+                      <div>
+                        <span>답변 전략</span>
+                        <p>{{ item.question.answer_guide || '경험 근거, 판단 기준, 검증 방법 순서로 답변을 정리하세요.' }}</p>
+                      </div>
+                      <div v-if="item.question.follow_up_questions.length">
+                        <span>꼬리질문</span>
+                        <ul>
+                          <li v-for="followUp in item.question.follow_up_questions" :key="followUp">{{ followUp }}</li>
+                        </ul>
+                      </div>
+                    </div>
+                  </article>
+                </div>
+              </section>
+            </div>
+            <p v-else class="roadmap-empty">상태별로 정리할 준비 항목이 없습니다.</p>
+          </section>
+
           <section class="section-card" id="roadmap" data-od-id="result-roadmap">
             <div class="section-head">
               <div>
@@ -147,6 +218,7 @@ const pageSections = [
   { id: 'gap', label: '역량 분석' },
   { id: 'scores', label: '근거 커버리지' },
   { id: 'prep-keywords', label: '준비 키워드' },
+  { id: 'action-plan', label: '액션 플래너' },
   { id: 'roadmap', label: '준비 항목' },
   { id: 'interview-drill', label: '질문 리허설' },
 ]
@@ -161,6 +233,7 @@ const {
   roadmapItems,
   toggleTask,
 } = useRoadmapProgress(analysis)
+const openStrategyKeys = ref({})
 
 const TYPE_LABELS = {
   culture_fit: '컬처핏', coding_test: '코딩테스트', pt: 'PT면접',
@@ -196,6 +269,56 @@ const interviewQuestionCount = computed(() => {
   }, 0)
 })
 
+const actionPlannerGroups = computed(() => {
+  const groups = [
+    {
+      type: 'appeal',
+      label: '어필 가능',
+      description: '직접 연결 경험을 면접 답변으로 압축합니다.',
+      items: [],
+    },
+    {
+      type: 'organize',
+      label: '답변 정리',
+      description: '유사 경험과 선택 근거를 직무 언어로 바꿉니다.',
+      items: [],
+    },
+    {
+      type: 'study',
+      label: '학습 필요',
+      description: '경험 공백을 개념과 적용 질문으로 보완합니다.',
+      items: [],
+    },
+  ]
+  const groupByType = Object.fromEntries(groups.map(group => [group.type, group]))
+
+  roadmapItems.value.forEach((category, categoryIdx) => {
+    category.subtopics.forEach((subtopic, subtopicIdx) => {
+      const type = ['appeal', 'organize', 'study'].includes(subtopic.preparation_type)
+        ? subtopic.preparation_type
+        : 'study'
+      const question = strategyQuestion(subtopic)
+      const completionQuestionIdx = 0
+      const togglePayload = { categoryIdx, subtopicIdx, questionIdx: completionQuestionIdx }
+      const key = `${categoryIdx}-${subtopicIdx}-${completionQuestionIdx}`
+      groupByType[type].items.push({
+        key,
+        category: category.category,
+        title: subtopic.title,
+        action: subtopic.approach || subtopic.study_goal || subtopic.job_reason || question.answer_guide,
+        question,
+        done: Boolean(completedTasks.value[key]),
+        togglePayload,
+      })
+    })
+  })
+
+  return groups.map(group => ({
+    ...group,
+    doneCount: group.items.filter(item => item.done).length,
+  }))
+})
+
 function hasEvidenceTrace(subtopic) {
   return Boolean(
     subtopic.evidence?.trim()
@@ -204,6 +327,28 @@ function hasEvidenceTrace(subtopic) {
   )
 }
 
+function strategyQuestion(subtopic) {
+  const questions = Array.isArray(subtopic.questions) ? subtopic.questions : []
+  const index = questions.findIndex(question => question.type === 'experience')
+  const fallbackIndex = index >= 0 ? index : 0
+  return {
+    index: fallbackIndex,
+    ...(questions[fallbackIndex] || {
+      question: subtopic.title,
+      answer_guide: subtopic.approach || subtopic.study_goal || '',
+      follow_up_questions: [],
+    }),
+  }
+}
+
+function toggleStrategy(key) {
+  openStrategyKeys.value[key] = !openStrategyKeys.value[key]
+}
+
+function isStrategyOpen(key) {
+  return Boolean(openStrategyKeys.value[key])
+}
+
 // Sidebar Active Navigation on Scroll
 function handleScroll() {
   const anchorOffset = 72
@@ -595,6 +740,157 @@ h2 {
   font-weight: 600;
 }
 
+.action-plan-card {
+  background:
+    linear-gradient(180deg, color-mix(in oklab, var(--surface), var(--bg) 54%), var(--bg));
+}
+.action-grid {
+  display: grid;
+  grid-template-columns: repeat(3, minmax(0, 1fr));
+  gap: var(--space-4);
+}
+.action-column {
+  border: 1px solid var(--border-soft);
+  border-radius: var(--radius-lg);
+  overflow: hidden;
+  background: var(--bg);
+  box-shadow: var(--elev-ring);
+}
+.action-head {
+  min-height: 112px;
+  display: flex;
+  align-items: flex-start;
+  justify-content: space-between;
+  gap: var(--space-3);
+  padding: var(--space-5);
+  background: var(--surface-warm);
+}
+.action-appeal .action-head {
+  background: color-mix(in oklab, var(--success), var(--bg) 82%);
+}
+.action-organize .action-head {
+  background: color-mix(in oklab, var(--warn), var(--bg) 82%);
+}
+.action-study .action-head {
+  background: color-mix(in oklab, var(--danger), var(--bg) 86%);
+}
+.action-head strong {
+  display: block;
+  color: var(--fg);
+  font-size: var(--text-base);
+  line-height: 1.35;
+}
+.action-head p {
+  color: var(--muted);
+  font-size: var(--text-xs);
+  line-height: 1.5;
+  margin-top: var(--space-1);
+}
+.action-head span {
+  flex: 0 0 auto;
+  border-radius: var(--radius-pill);
+  padding: 4px 9px;
+  background: color-mix(in oklab, var(--bg), transparent 12%);
+  color: var(--fg);
+  font-family: var(--font-mono);
+  font-size: var(--text-xs);
+  font-weight: 600;
+}
+.action-items {
+  display: grid;
+  gap: var(--space-3);
+  padding: var(--space-4);
+}
+.action-item {
+  border-bottom: 1px solid var(--border-soft);
+  padding-bottom: var(--space-3);
+}
+.action-item:last-child {
+  border-bottom: 0;
+  padding-bottom: 0;
+}
+.action-check-row {
+  display: grid;
+  grid-template-columns: 20px 1fr;
+  align-items: start;
+  gap: var(--space-3);
+  cursor: pointer;
+}
+.action-check-row input {
+  width: 18px;
+  height: 18px;
+  margin-top: 2px;
+  accent-color: var(--accent);
+}
+.action-check-row span {
+  display: grid;
+  gap: 2px;
+}
+.action-check-row strong {
+  color: var(--fg);
+  font-size: var(--text-sm);
+  line-height: 1.4;
+  word-break: keep-all;
+}
+.action-check-row em {
+  color: var(--muted);
+  font-size: var(--text-xs);
+  font-style: normal;
+}
+.action-item > p {
+  color: var(--muted);
+  font-size: var(--text-xs);
+  line-height: 1.55;
+  margin: var(--space-2) 0 0 32px;
+}
+.action-item.done .action-check-row strong {
+  color: var(--muted);
+  text-decoration: line-through;
+}
+.strategy-toggle {
+  min-height: 30px;
+  margin: var(--space-3) 0 0 32px;
+  border: 1px solid var(--border);
+  border-radius: var(--radius-pill);
+  padding: 5px 12px;
+  background: var(--surface);
+  color: var(--accent);
+  font-size: var(--text-xs);
+  font-weight: 600;
+  transition: background var(--motion-fast) var(--ease-standard), transform var(--motion-fast) var(--ease-standard);
+}
+.strategy-toggle:hover {
+  background: var(--bg);
+}
+.strategy-toggle:active {
+  transform: scale(0.98);
+}
+.strategy-panel {
+  display: grid;
+  gap: var(--space-3);
+  margin: var(--space-3) 0 0 32px;
+  padding: var(--space-4);
+  border: 1px solid var(--border-soft);
+  border-radius: var(--radius-md);
+  background: var(--surface-warm);
+}
+.strategy-panel span {
+  display: block;
+  color: var(--accent);
+  font-size: var(--text-xs);
+  font-weight: 700;
+  margin-bottom: var(--space-1);
+}
+.strategy-panel p,
+.strategy-panel li {
+  color: var(--fg-2);
+  font-size: var(--text-xs);
+  line-height: 1.6;
+}
+.strategy-panel ul {
+  padding-left: var(--space-4);
+}
+
 .roadmap-text {
   white-space: pre-wrap;
   font-family: inherit;
@@ -627,6 +923,9 @@ h2 {
     grid-column: 1 / span 2;
     width: 100%;
   }
+  .action-grid {
+    grid-template-columns: 1fr;
+  }
 }
 @media (max-width: 640px) {
   .section-head {
diff --git a/frontend/tests/e2e/analyze-flow.spec.js b/frontend/tests/e2e/analyze-flow.spec.js
index 55acf6b..5b21df2 100644
--- a/frontend/tests/e2e/analyze-flow.spec.js
+++ b/frontend/tests/e2e/analyze-flow.spec.js
@@ -40,13 +40,22 @@ test('analyze flow saves manual posting, cover letter, submits, and renders resu
   await page.locator('#next-cover-letter-btn').click()
 
   await expect(page).toHaveURL(/\/analyze\/99$/)
-  await expect(page.getByRole('heading', { name: '역량 분석' })).toBeVisible()
+  await expect(page.getByRole('heading', { name: '역량 지도' })).toBeVisible()
   const competencySection = page.locator('#gap')
   await expect(competencySection.getByText('어필 가능', { exact: true })).toBeVisible()
   await expect(competencySection.getByText('답변 정리', { exact: true })).toBeVisible()
   await expect(competencySection.getByText('학습 필요', { exact: true })).toBeVisible()
   await expect(page.getByText('API 성능 개선').first()).toBeVisible()
   await expect(page.getByText('시스템 설계').first()).toBeVisible()
+  await expect(page.getByText('현재 역량', { exact: true })).toBeVisible()
+  await expect(page.getByText('직무 요구', { exact: true })).toBeVisible()
+  const actionPlan = page.locator('#action-plan')
+  await expect(actionPlan.getByRole('heading', { name: '상태별 액션 플래너' })).toBeVisible()
+  await expect(actionPlan.getByText('1/1 완료')).toBeVisible()
+  const strategyButton = actionPlan.getByRole('button', { name: /답변 전략 보기/ }).first()
+  await strategyButton.click()
+  await expect(actionPlan.getByText('답변 전략', { exact: true }).first()).toBeVisible()
+  await expect(actionPlan.getByText('문제, 구현, 검증 순서로 설명하세요.')).toBeVisible()
   await expect(page.getByRole('heading', { name: '직무 역량 매칭도' })).toHaveCount(0)
   await expect(page.getByRole('heading', { name: '준비 항목' })).toBeVisible()
   await expect(page.getByRole('heading', { name: '로보틱스' })).toBeVisible()
diff --git a/llm_server/roadmap_prompt.py b/llm_server/roadmap_prompt.py
index 63ae421..6ba0f9c 100644
--- a/llm_server/roadmap_prompt.py
+++ b/llm_server/roadmap_prompt.py
@@ -88,6 +88,7 @@ def build_prompt(req) -> str:
    - study: 직무에 필요하지만 경험 또는 지식 근거가 없어 학습해야 함
    - insufficient_data: 입력이 부족하여 판단할 수 없음
 4. competency_map의 keyword와 signal은 한눈에 읽히도록 짧게 작성하고, 상세한 경험 설명은 넣지 마세요.
+4-1. competency_map의 action은 "어필", "학습" 같은 단어로 끝내지 말고, 면접 답변 행동 또는 학습 행동을 12~24자 안팎의 구체적인 동사구로 작성하세요.
 5. 강점은 "역량 이름"이 아니라 면접에서 활용할 수 있는 경험 중심의 2~6어절 핵심 키워드로 작성하세요. 예: "주문 API 성능 개선", "팀 갈등 조율 경험".
 6. 강점마다 어떤 경험인지 experience에 쓰고, 입력에서 확인된 사실을 evidence에 한 문장으로 요약하며, 해당 직무에서 왜 유효한지 job_relevance에 작성하세요.
 7. 보완 역량은 다음 유형 중 하나로 정확히 구분하세요.
@@ -155,7 +156,7 @@ def build_prompt(req) -> str:
         "status": "strength | articulate | study | insufficient_data",
         "importance": "required | preferred",
         "signal": "경험 있음, 설명 보완, 지식 부족처럼 한눈에 보는 짧은 판단",
-        "action": "어필, 답변 정리, 학습, 정보 보완 중 다음 행동"
+        "action": "면접 답변 행동 또는 학습 행동 중심의 다음 행동"
       }}
     ],
     "strengths": [
diff --git a/llm_server/tests/test_roadmap_prompt.py b/llm_server/tests/test_roadmap_prompt.py
index 83d7101..7a1a065 100644
--- a/llm_server/tests/test_roadmap_prompt.py
+++ b/llm_server/tests/test_roadmap_prompt.py
@@ -39,3 +39,4 @@ def test_prompt_requires_company_context_personal_evidence_and_followups():
     assert all(token in prompt for token in ['experience 유형 질문', 'matched_experience', 'experience_connection.evidence'])
     assert all(token in prompt for token in ['follow_up_questions', '최소 1개', '직무 이해도'])
     assert all(token in prompt for token in ['STAR', '사용할 경험 근거', '핵심 기술 판단'])
+    assert all(token in prompt for token in ['competency_map의 action', '면접 답변 행동', '학습 행동'])
